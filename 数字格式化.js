// 1234567890 --> 1,234,567,890
const fn = (number)=>{
    let str = number.toString()
    let arr = str.split('')
    arr.reverse()
    for
    
}

// 请为所有数组对象添加一个findDuplicate(n)方法，用于返回该数组中出现频率>=n的元素列表